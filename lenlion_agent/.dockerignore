# Virtual envs / caches
.venv/
venv/
.uv-cache/
.pip-cache/
**/__pycache__/
**/*.py[cod]
.pytest_cache/
*.egg-info/

# Node / web build (web_dist is rebuilt in the web-builder stage)
node_modules/
**/node_modules/
web/dist/
hermes_cli/web_dist/

# VCS / CI / tests / docs-only assets
.git/
.github/
tests/
assets/

# Runtime state (lives in the /data volume, never in the build context)
.hermes/
logs/
data/

# Secrets
.env
.env.*

.DS_Store
