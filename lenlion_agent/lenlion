#!/usr/bin/env python3
"""
Lenlion Agent CLI launcher.

This wrapper should behave like the installed `lenlion` command, including
subcommands such as `gateway`, `cron`, and `doctor`.
"""

if __name__ == "__main__":
    from hermes_cli.main import main
    main()
